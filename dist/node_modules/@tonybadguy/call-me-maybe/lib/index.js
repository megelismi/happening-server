'use strict';

var _smallRequest = require('small-request');
var _defaultRequestFilters = require('./default-request-filters');
var _defaultResponseFilters = require('./default-response-filters');
var _processFilters = require('./process-filters');

var send = function send(request, requestFilters, responseFilters) {

  if (!requestFilters) {
    requestFilters = _defaultRequestFilters();
  }

  if (!responseFilters) {
    responseFilters = _defaultResponseFilters();
  }

  return _smallRequest.send(_processFilters(request, requestFilters)).then(function (response) {
    return _processFilters(response, responseFilters);
  });
};

module.exports = send;
//# sourceMappingURL=index.js.map