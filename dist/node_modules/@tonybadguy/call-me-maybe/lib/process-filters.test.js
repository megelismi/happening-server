'use strict';

var _test = require('tap').test;
var _processFilters = require('./process-filters');

_test('Calls all filters in order', function (assert) {

  assert.plan(2);
  var expectedContext = {};

  var filter1 = {
    filter: function filter(context) {
      assert.equal(context, expectedContext);
      return context;
    }
  };

  var filter2 = {
    filter: function filter(context) {
      assert.equal(context, expectedContext);
      assert.end();
      return context;
    }
  };

  _processFilters(expectedContext, [filter1, filter2]);
});
//# sourceMappingURL=process-filters.test.js.map