'use strict';

var _headers = require('../headers');

var filter = function filter(request) {

  if (request.bearerToken) {
    _headers.setHeader(request, 'Authorization', 'Bearer ' + request.bearerToken);
  }

  return request;
};

module.exports = {
  filter: filter
};
//# sourceMappingURL=bearer-token.js.map