'use strict';

var _test = require('tap').test;
var _defaultOptions = require('./default-options');

_test('GET method by default', function (assert) {
  var result = _defaultOptions.filter({});

  assert.equals(result.method, 'GET');
  assert.end();
});

_test('Defaults do not override setting', function (assert) {
  var result = _defaultOptions.filter({
    method: 'POST'
  });

  assert.equals(result.method, 'POST');
  assert.end();
});
//# sourceMappingURL=default-options.test.js.map