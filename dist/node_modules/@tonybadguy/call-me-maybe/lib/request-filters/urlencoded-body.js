'use strict';

var _caseless = require('caseless');
var _querystring = require('querystring');
var _headers = require('../headers');

var contentType = 'application/x-www-form-urlencoded';

var filter = function filter(request) {

  if (request.urlencodedBody) {

    request.body = _querystring.stringify(request.urlencodedBody);
    _headers.setContentTypeIfNotExist(request, contentType);
  }

  return request;
};

module.exports = {
  filter: filter
};
//# sourceMappingURL=urlencoded-body.js.map