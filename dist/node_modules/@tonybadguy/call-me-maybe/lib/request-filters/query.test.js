'use strict';

var _test = require('tap').test;
var _query = require('./query');

_test('Noop on basic Url', function (assert) {
  var result = _query.filter({
    url: 'https://www.google.com'
  });

  assert.equals(result.url, 'https://www.google.com/');
  assert.end();
});

_test('Update request url', function (assert) {
  var result = _query.filter({
    url: 'https://www.google.com',
    query: {
      foo: 'oh hai',
      bar: 'kthxbai'
    }
  });

  assert.equals(result.url, 'https://www.google.com/?foo=oh%20hai&bar=kthxbai');
  assert.end();
});

_test('query unchanged', function (assert) {
  var query = {
    foo: 'oh hai'
  };
  var result = _query.filter({
    url: 'https://www.google.com',
    query: query
  });

  assert.equals(query.foo, 'oh hai');
  assert.end();
});
//# sourceMappingURL=query.test.js.map