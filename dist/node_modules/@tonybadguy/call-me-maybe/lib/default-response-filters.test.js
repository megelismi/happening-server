'use strict';

var _test = require('tap').test;
var _defaultResponseFilters = require('./default-response-filters');
var _unhandledStatus = require('./response-filters/unhandled-status');
var _jsonBody = require('./response-filters/json-body');

_test('Returns correct filters', function (assert) {
  var defaultResponseFilters = _defaultResponseFilters();

  var expected = [_unhandledStatus, _jsonBody];

  assert.equal(defaultResponseFilters.length, expected.length);
  for (var i = 0; i < expected.length; i++) {
    assert.equal(defaultResponseFilters[i], expected[i]);
  }
  assert.end();
});
//# sourceMappingURL=default-response-filters.test.js.map